# model-trainer
Thin wrapper around PyTorch training loop with sane defaults.

## Documentation for random_seed propagation

This document outlines how the random seed is propagated across the system.